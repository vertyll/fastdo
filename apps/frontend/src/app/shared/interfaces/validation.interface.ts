export interface MessagesConfig {
  required: string;
  email: string;
  min: string;
  max: string;
  custom: string;
  maxlength: string;
  passwordMatch: string;
  pattern: string;
  minlength: string;
  mismatchingPasswords: string;
}
